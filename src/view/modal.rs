use crate::app::App;
use crate::model::modal_state::ModalState;
use ratatui::{Frame, layout::Rect};

pub fn render(_app: &App, _frame: &mut Frame, _modal: &ModalState, _area: Rect) {
    // TODO: Implement modal rendering
}
